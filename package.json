{
  "name": "sokki",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "//engines": "scripts/check-built.mjs imports the locale modules as TypeScript. Node strips types without a flag from 22.18; below that the build dies on ERR_UNKNOWN_FILE_EXTENSION with nothing saying why.",
  "engines": { "node": ">=22.18.0" },
  "scripts": {
    "dev": "vite",
    "check": "npm run lint && npm run typecheck && npm run test",
    "build": "node scripts/make-icons.mjs && npm run check && vite build && node scripts/seal-csp.mjs && node scripts/check-built.mjs && node scripts/check-budget.mjs",
    "preview": "vite preview",
    "lint": "oxlint --type-aware",
    "lint:fix": "oxlint --type-aware --fix",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "icons": "node scripts/make-icons.mjs",
    "budget": "node scripts/check-budget.mjs",
    "og": "node scripts/check-og.mjs"
  },
  "dependencies": {
    "preact": "^10.29.8"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.6",
    "@types/node": "^26.3.0",
    "oxlint": "^1.81.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vite-plugin-pwa": "^1.3.0",
    "vitest": "^4.1.11",
    "wrangler": "^4.127.0"
  }
}
