# Optional. Without it the app runs unchanged and simply reports nothing.
#
# Where monitoring beacons are sent — errors, and timings that exceeded their
# budget. Never card content, never a spreadsheet id. Leave it unset unless you
# have somewhere to receive them, such as a Cloudflare Pages Function.
#
# Traffic and Core Web Vitals do not come through here: those are Cloudflare
# Web Analytics, which you turn on in the Pages dashboard, and which sets no
# cookie and no identifier.
VITE_TELEMETRY_ENDPOINT=

# Optional but recommended. The absolute origin the site is served from, used to
# make og:url and og:image absolute — a link preview with a relative image is at
# the mercy of whether a given crawler resolves it, and none promise to.
#
#   VITE_SITE_ORIGIN=https://sokki.example.workers.dev
#
# Set it in the Cloudflare build environment, not here.
VITE_SITE_ORIGIN=
