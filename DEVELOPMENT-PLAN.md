<!--
  A record of what was decided before any code was written, and why.

  Kept as it was rather than rewritten to match the build. Its value is the
  reasoning, and reasoning is only checkable against the moment it was made.
  Where the code has since diverged, the divergence is listed at the end with
  its own reason — see 計畫之後的變更.
-->

# 一條連結就是一副牌

零基建、零帳號、零使用者資料的速記卡工具。Google Sheets 是唯一資料來源且唯讀， URL 是唯一的狀態容器。

> 這不是間隔重複工具，是操練工具。沒有排程、沒有進度、沒有明天回來的理由——這是刻意的取捨，換來的是沒有任何一份使用者資料需要我們保管。
>
> 文案與命名都必須誠實反映這件事。暗示它會幫你排程，使用者的期待就會落空。

## 核心決策

### 產品：操練工具，不是 SRS

拿掉持久化排程，就沒有「今天該複習 N 張」，也沒有每日回訪機制。完成感改由**單次工作階段**提供：答錯的卡可以重考到全對。這本質上是萊特納盒壓縮進一次工作階段，零儲存成本。

### 資料：Sheets 唯讀，App 永不寫入

使用者在 Google Sheets 編輯內容，App 只讀。因此不需要 OAuth、不需要 Google 審核、沒有寫壞使用者試算表的風險，也沒有卡片身分識別問題—— 沒有持久化的複習進度，就沒有東西會被錯誤指派。

**結構收斂到極限：`A` 欄正面、`B` 欄背面，沒有標題列。**第一列就是第一張卡。提示欄、音訊欄、圖片、停用列約定全部不做—— 實測確認 Google Sheets 的儲存格內圖片在 gviz CSV 裡是空值，讀不到，所以「上傳圖片當卡面」這條路從一開始就不存在。純文字進、純文字出，沒有任何外部資源，答案欄也不解析 markdown。

### 隱私：不留存任何個人化數據

這條原則決定了監控方案：Cloudflare Web Analytics 沒有 cookie、沒有識別碼、免同意橫幅。GA4 因為架構上必然需要持久性 `client_id`，與這條原則直接衝突而被排除——順帶省下 146 KB gzip。

### URL：Query param，三個參數

`?s=` 試算表 ID、`&g=` gid（選用，預設 0）、 `&t=` 牌組名稱（選用）。不用 path routing——為了好看去換一個部署設定的相依不划算，而 query param 在任何靜態主機上零設定即可運作。未知參數一律忽略，未來加參數完全向後相容。

### 命名：牌組名稱放 URL，不放試算表

gviz 的回應裡沒有標題欄位（已實測），所以名稱只能由分享者提供。放 URL 的三個好處：同一份表可以針對不同對象取不同名稱、不強迫使用者改動既有表格結構、**而且名稱在 Sheet 載入前就能顯示**，感知速度直接變快。用於分頁標題；安裝後的 App 名稱維持固定。

### 儲存：三層，職責分離

**Cache API + Service Worker** 快取 gviz 回應（stale-while-revalidate，保留最近 20 副牌）——因為 gviz 有 CORS 標頭，回應不是 opaque，App 端幾乎不用寫快取程式碼。 **localStorage** 分兩類鍵：`settings`（全域：語言、主題）與 `prefs:<id>:<gid>`（每副牌：方向、題數、亂序）。**工作階段**存在 `session:<id>:<gid>`，存索引陣列而非卡片物件，24 小時後失效，最多保留 10 份。

### 互動：滑動為主，但按鈕永遠存在

右滑＝我會、左滑＝我不會，設定控制的是「要不要啟用滑動」，**不是「要不要有按鈕」**——滑動不可見、鍵盤按不到、螢幕閱讀器讀不到，把唯一的操作方式藏在手勢裡會排除輔助科技使用者。 iOS 返回手勢的衝突用**卡片左右各內縮 16px** 解決：手指不從邊緣區起始就不會觸發，純 CSS 搞定，不需要 JS 攔截。安裝成 PWA 後 standalone 模式根本沒有返回手勢。

### 技術棧：寫 React，跑 Preact，保留退路

Vite + TypeScript + Preact，並在 Vite 裡把 `react` / `react-dom` 別名指向 `preact/compat`。你照 React 的寫法寫、用 React 的型別，打包出 8.5 KB 而不是 47 KB；未來想換回 React 只要刪掉三行別名。 compat 層同時弭平了 Preact 原生 DOM 事件與 React 合成事件的 `onChange` 語意差異——那是從 React 過來唯一真正會踩的坑。

### 字型：CJK 一律只用系統字型

三語系的真正效能風險在字型，不在 JS——完整的繁中／日文網頁字型是 3–15 MB，而且**子集化救不了你**：卡片內容是使用者任意輸入的文字，無法預先知道會用到哪些字。這是唯一能同時滿足「三語系」與「手機優先高效」的答案。

### 部署：Cloudflare Pages

之後若要補分享連結的 Open Graph 預覽或 per-deck manifest，只是加一個檔案，不必搬家； Web Analytics 也在同一個後台。GitHub Pages 免費額度一樣夠，但會把這兩個未來選項都堵死。預設 Brotli 壓縮，實際傳輸量會比下面的 gzip 預算再低 15–20%。

## 效能預算

驗收標準永遠是時間，不是位元組。但時間預算沒辦法在 CI 裡檢查——需要真實裝置與網路。位元組預算的價值就是「便宜的代理指標」，能在每次 build 自動擋下不必要的依賴。它在設計過程中已經擋掉三次：React 47 KB、i18next 40 KB、gtag.js 146 KB。

| 項目 | gzip | 來源 |
|---|---|---|
| Preact + compat | 9 KB | 實測 6.4 + compat 約 2 |
| web-vitals | 2.5 KB | 實測 2.4 |
| CF Analytics beacon | 1 KB |  |
| i18n 機制 + 當前語系字串 | 3 KB | 只載入使用中的語系 |
| CSV 解析 + Sheets 讀取 | 4 KB |  |
| 測驗流程與狀態機 | 8 KB |  |
| 主題與設定 | 3 KB |  |
| UI 元件 | 10 KB |  |
| 小計（App 的自然體積） | 40.5 KB |  |
| 預留 | 19.5 KB |  |
| 初始 JS 上限 | 60 KB | 自然體積的 1.5 倍 |

| 時間指標 | 目標 | 為什麼 |
|---|---|---|
| LCP（4G） | ≤ 1.5 s | 真正的驗收標準 |
| INP | ≤ 100 ms | 同上 |
| CLS | ≈ 0 | 主題閃爍是唯一風險，用 head 內同步 script 消除 |
| 翻牌到下一張 | ≤ 50 ms | **這組才是使用者真正感受到的** |
| 5,000 張卡建立題目佇列 | ≤ 200 ms | Core Web Vitals 量不到這個 |
| 解析 1,000 列 CSV | ≤ 1 s | 同上 |

> 規則：任何超過 10 KB 的新依賴需要書面理由。等 CF Web Analytics 累積到實測的 CWV 欄位資料，就用真實數字回頭修正上面那張位元組表——它只是在那之前的替代品。

## 刻意不做的事

這份清單和上面的決策一樣重要。每一項都是討論過、且因為具體理由被排除的，不是還沒想到。

| 不做 | 理由 |
|---|---|
| **間隔重複排程** | 換來零使用者資料。代價是沒有每日回訪機制。 |
| **複習進度持久化** | 沒有要保管的資料，就沒有要道歉的資料遺失。 |
| **試算表統計欄（通過／失敗次數）** | 手動維護必然中斷；自動寫回會把 OAuth 與位置身分的資料損毀風險全部帶回來。 |
| **OAuth 與 Drive 寫入** | 公開 Sheet + 唯讀 = 零授權、零審核、零同意畫面。 |
| **工作表當資料夾** | Sheets v3 feeds API 已死（實測 404），無 OAuth 無法列舉分頁。 |
| **朗讀（TTS）與音訊欄** | 系統語音品質會主動教錯發音，且 iOS 需要使用者手勢、無法自動播放、無法快取。使用者自備音檔也不做——最可能的來源 Google Drive 不是可播放的媒體 URL。 |
| **提示欄** | 兩段式翻開會動到狀態機與滑動設計。互動成本大於實作成本。 |
| **圖片渲染** | 已實測：Google Sheets 儲存格內的圖片在 gviz CSV 是空值。要做就得改放網址，等於把音訊欄的每個失效模式再走一遍。 |
| **標題列處理** | 沒有標題列這個概念，第一列就是第一張卡。複雜度移到設定說明文件，不進程式碼。 |
| **答案欄的 markdown** | 純文字渲染。少一個依賴，也少一個 XSS 面。 |
| **每副牌獨立的 PWA 圖示** | 依賴兩個未經真機驗證的瀏覽器行為，加上一個已推遲的邊緣函式。 |
| **per-deck 分享預覽** | 純靜態站無法為每個牌組產生 OG 標籤。v1 接受通用預覽。 |
| **使用者自訂主題顏色** | 必然產出無法通過對比度檢查的組合。 |
| **商用與 App Store** | Apple 開發者帳號每年 US$99 直接推翻「0 營運費用」。 |

## 建置順序

順序本身帶著兩個判斷：**預算閘門要在第 1 階段就進 CI**（放到最後就永遠不會加），而 **i18n 的字串抽離要在第 1 階段做、實際翻譯留到最後**（事後補抽離很痛苦，翻譯只是機械工作）。

### 01 · 骨架與閘門

- Vite + TS + Preact，`react` → `preact/compat` 別名
- 顏色 token 架構、`data-theme`、head 內同步 script 消除閃爍
- 白／黑兩個主題、`meta theme-color` 隨主題更新
- i18n 骨架：`t()`、每語系動態 import、字串全部抽離
- 部署到 Cloudflare Pages

### 02 · 讀取路徑

- Sheets URL 解析——容忍完整編輯網址、`#gid=`、App 分享連結、裸 ID
- CSV 解析——引號內逗號／換行、雙引號轉義、BOM、空列
- gviz 讀取與錯誤狀態（未公開、找不到、空表、只有標題列）
- CSV 上傳作為完全私密的替代路徑
- 內建示範牌組走同一條解析路徑，只是換一個來源 URL

### 03 · 測驗流程

- 亂序與抽題（題數大於／小於／等於牌組張數的邊界）
- 翻面、自我評分、答錯清單
- 結束畫面：總數、第一次就答對的張數、答錯的卡片列表
- 結束時**詢問**使用者：重新開始，或只重練答錯的
- 工作階段寫入 localStorage，中離可續

### 04 · 手機互動

- 滑動手勢（卡片跟隨手指、25% 門檻、顏色提示、首次一次性提示）
- 卡片左右內縮 16px 解 iOS 返回手勢衝突
- 底部拇指區按鈕永存，`min-height: 48px`，`env(safe-area-inset-*)`
- `touch-action: pan-y`、監聽器 `passive: true`
- 桌面鍵盤快捷鍵；單一斷點約 640px

### 05 · 離線與 PWA

- `vite-plugin-pwa` + Workbox，gviz 走 stale-while-revalidate
- 離線標示「顯示上次載入的版本」
- 背景更新完成時提示「下次測驗生效」，**不中途抽換正在做的題目**
- manifest、安裝引導（這是 iOS 上唯一能避免資料被清除的方法）

### 06 · 監控

- Cloudflare Web Analytics（流量 + CWV，無 cookie）
- `web-vitals`，`visibilitychange` 時用 `sendBeacon` 送出
- `onerror` + `unhandledrejection`，同訊息每工作階段只報一次
- `performance.mark/measure`：讀取、解析、建佇列、翻牌延遲，超過門檻才上報

### 07 · 分享與首頁

- 分享流程：取名 → 產生連結 → 一鍵複製，使用者永遠看不到 `?s=`
- 首頁：一句說明、一個貼連結的輸入框、一個「試試看範例」
- 四副示範牌組：日→英、英→中、中→日 的語言三角，加上 47 張的日本県庁所在地
- 範例牌組走**和一般牌組完全相同的解析與測驗路徑**，只有來源 URL 不同
- Sheet 設定說明：A 欄正面、B 欄背面、**不要放標題列**、共用設為知道連結的任何人可檢視

### 08 · 補完

- 剩下 8 種主題配色
- en 與 ja 語系的實際翻譯
- 對比度閘門：任何未通過 WCAG AA 的主題組合不准進版

## 已查證的事實

| 來源 | 事實 |
|---|---|
| 已實測 | gviz CSV 端點**支援 CORS**——回應會 echo 請求的 `Origin` （只在請求帶 Origin 時出現，這正是瀏覽器 fetch 的情形）。所以純靜態頁面可以直接讀公開試算表，零 OAuth。 |
| 已實測 | gviz JSON 的欄位只有 `version / reqId / status / sig / table`，**沒有試算表標題或工作表名稱**。牌組名稱只能由 URL 提供。 |
| 已實測 | Sheets v3 feeds API **回應 404**，這是唯一能不經 OAuth 列舉工作表的端點。 `gviz` 接受 `sheet=名稱`（200 text/csv），但你得先知道名字。 |
| 官方文件 | `drive.file` 與 `drive.appdata` 是**非敏感** scope（免審核、無 100 人上限）； `spreadsheets` 與 `spreadsheets.readonly` 是**敏感** scope—— 聽起來更窄，實際上會把你鎖在 100 人上限或 4–6 週人工審核。 `spreadsheets.get` 接受 `drive.file`。 |
| 官方文件 | Sheets API 配額：每使用者每分鐘 60 次、每專案每分鐘 300 次（全體共用），無每日上限。本專案不呼叫 Sheets API，此項僅供未來若走 OAuth 路徑時參考。 |
| 已量測 | gzip 體積：Preact 10 **4.8 KB**、preact/hooks 1.5 KB、 React 18 + react-dom **47 KB**、web-vitals **2.4 KB**、 gtag.js **146 KB**（raw 418 KB）。 |
| 未驗證 | manifest 的 `id` 確實能區分 PWA 身分。但**無法確認** Chrome 的安裝流程是否可靠接受 `data:` URI manifest 或執行期抽換的 manifest 連結，也無法確認 iOS 是否採用 JS 動態設定的 `apple-mobile-web-app-title`。需要真機測試——這是「每副牌獨立圖示」被推遲的原因。 |

## 示範牌組

四副，各 25–47 張。前三副組成 **日 → 英 → 中 → 日** 的方向三角，三個互不相干的主題，剛好對上三個 UI 語系。第四副是非語言領域，任務是**用非翻譯型的問答壓測版面**——三副單字牌的答案都只有幾個字，永遠測不出長答案會不會撐爆卡片。

| 牌組 | 方向 | 張數 | 示範什麼 |
|---|---|---|---|
| 日文 N5 動詞 | 日 → 英 | 25 | 唯一含 ASCII 逗號的一副，會壓到 RFC4180 引號解析 |
| 多益商用單字 | 英 → 中 | 25 | 答案最短只有 2 個字元 |
| 中日常會話 | 中 → 日 | 25 | 短句對短句 |
| 日本県庁所在地 | 県 → 市 | 47 | 非語言領域；唯一大到能測「題數 < 牌組張數」的抽題邏輯 |

> 示範牌組以靜態 CSV 隨站部署，用 `?d=<id>` 載入。解析與測驗流程和使用者自己的試算表完全共用，差別只有來源 URL—— 這樣示範牌組離線可用，也不需要我們去維護四份公開的 Google Sheet。


---

## 計畫之後的變更

這份計畫寫在動手之前。以下是實作過程中偏離計畫的地方，以及偏離的理由。上面的內容維持原樣——那是當時的判斷，改寫它會讓這份文件失去可對照的價值。

### 部署改到 Cloudflare Workers

計畫寫的是 Pages。Cloudflare 之後把 Pages 轉為維護模式，官方指引新專案使用 Workers。Workers 的靜態資產原生支援 `_headers`，所以 CSP 與快取策略原封不動沿用，遷移成本為零。設定收在 `wrangler.jsonc`。

### 移除 CSV 上傳

計畫的第 2 階段有「CSV 上傳作為完全私密的替代路徑」。後來拿掉了，理由是它讓「每副牌都是一個可分享的 URL」這個核心機制出現例外——上傳的檔案沒有網址，因此不能分享，而分享正是這個產品的形狀。移除後 `DeckRef` 少一種型別，`refToQuery` 與 `shareUrl` 不再需要回傳 `null`。

代價要說清楚：**現在沒有私密路徑了**，每副牌都必須是公開的 Google Sheet。

### 「顯示答案」改成翻面

計畫沒有提到卡片有兩面。實作時改成真正的兩面卡：點擊或空白鍵翻面，再按翻回去。評分**不需要**先翻面——已經會的卡直接送出才是常態，強迫人看一眼不需要的答案是摩擦而不是保護。

### 首頁改成兩個入口加 modal

計畫的第 7 階段寫「一個『試試看範例』」。實際做成兩列可點擊的入口（範例、試算表怎麼做），各自開一個蓋在頁面上的卡片 modal。原本兩塊常駐面板收起來之後，首頁只剩三個元素。

### 滑動手勢比計畫細

計畫只寫了「滑動」。實作後經過兩輪調整：加入方向鎖定（避免捲動長答案時誤觸），再加入速度觸發與**依內容決定的方向閘門**——可捲動的卡片用嚴格比例保護捲軸，不可捲動的卡片放寬，因為那裡沒有捲軸需要保護。

### 十種主題一次做完

計畫寫「先出白與黑，其餘 8 種之後補」。實際一次做完，因為 token 架構就緒後每個主題只是一組值，而對比度閘門讓新增主題的風險趨近於零。

### 加入可選的 Markdown（計畫外）

計畫完全沒有提到格式化，卡片就是兩個字串。後來加了一個極小的 Markdown 子集，用 `&md=1` 帶在分享網址裡、每副牌各自記住，預設關閉。

做的：`**粗體**`、`*斜體*`、`` `程式碼` ``、`~~刪除線~~`、`#`～`######` 標題、`>` 引言、`-` 清單、`---` 分隔線。

刻意不做的，以及理由：

- **`_` 系列的強調**——只認 `*`。CommonMark 之所以要為 `_` 訂單字邊界規則，就是為了讓 `snake_case` 活下來；規則寫錯就會吃掉識別字，而完全不認 `_` 不可能寫錯。
- **反斜線跳脫**——改用 CommonMark 的 flanking 規則，`2 * 3 * 4` 因此原樣保留。代價說清楚：**沒有任何方法可以顯示字面上的 `*重點*`**。
- **連結與圖片**——連結的點擊會跟翻面的點擊打架，而且是釣魚面。
- **表格與程式碼區塊**——兩者需要的寬度卡片給不起，唯一的補救是卡片內橫向捲動，那會跟回答用的滑動打架。語法高亮另外算：Prism 核心實測 17.4 KB gzip，等於整個 App 的體積，而且顏色是第二套色盤，要在十個主題上重新設計並過對比度閘門。

兩個跨切面的決定：

- **換行全部保留**，不照 Markdown 慣例合併。打開 `md=1` 不應該讓別人已經排好的內容悄悄重排。唯一的例外是**只用來隔開兩個區塊的空行**——區塊之間本來就有間距，留著會讓清單離標題比其他東西遠一倍。
- **含區塊語法的那一面改為靠左**並降到內文級字級。置中的項目符號清單無法閱讀，而問題面的 30px 配上標題會讓版面直接爆開。只有行內語法的卡片——也就是絕大多數——維持原本的置中與字級。

**沒有任何一步產生 HTML 字串**。解析器輸出資料結構，`ui/CardText` 把它轉成 Preact 元素。卡片內容來自任何人都能編輯的公開試算表，所以是不可信輸入；安全性來自「注入沒有路徑可走」，而不是「注入被過濾掉」。實測 `<script>alert(1)</script>` 這張卡渲染出的是那串字元，卡片內 script 元素數為 0。

體積：初始 JS 從 17.5 KB 增加到 **18.6 KB**（+1.1 KB gzip），比事前估的 4 KB 少。

### 主題重新設計：問題不在色相，在幾何

計畫寫「10 種左右」的多色主題，做完後十個裡有八個像同一個主題的十次微調。原因是可量測的：**每個主題的底色都貼在明度的極端，而 sRGB 在那裡幾乎裝不下彩度**。底色彩度實測 0–11（滿分 255），只有 sepia 的 27 有真顏色——而它也是唯一讀起來像顏色的。所有彩度都堆在 `--accent`，而 accent 只畫一顆按鈕和一條進度條。

移除 forest、ocean、plum、sand、slate、rose、mono；新增 sakura、coral、steel、terminal、amber、dracula、matcha。有顏色的主題離開極端換取彩度，light/dark 刻意留在極端當中性基準。`steel` 和 `terminal` 的底色彩度接近零是刻意的——金屬本來就無彩，終端機是黑螢幕加彩色文字。**重點從來不是每個主題都要鮮豔，是每個主題要能表達什麼，而近白的底色表達不了。**

light 的 accent 從綠改成黑，good/bad 改的是**彩度不是明度**——它們被夾在「當文字要 4.5」和「白字放上去也要 4.5」之間，那才是它們原本暗沉的原因。

途中修掉兩個安靜的缺陷：**移除一個主題會把使用者的語言和滑動設定一起清掉**（`isSettings` 在 theme 不認得時整筆作廢），以及行內防閃腳本會把不存在的 `data-theme` 蓋上去。另外 `.notice.bad` 把 `--ink` 放在 `--bad-soft` 上，那是唯一沒被對比閘門檢查的文字配對，現在是 11 組而不是 10 組。

### 優化：量測推翻了三個假設

計畫沒有優化階段。這一輪的結論是**這個 App 本來就不慢**，而三次基於直覺的改動都被自己的量測否決：

- **記憶體「更快釋放」** —— 沒有東西可釋放。3000 張卡只比 50 張多 0.94 MB（每張約 320 位元組，就是兩個字串），答 120 張卡後 heap 增加 0.02 MB、節點 +1、監聽器 +0。第一次量到的 72 MB / 1175 個監聽器是 CDP session 的量測雜訊——先做基準對照才看出來。
- **把拖曳的 `dx` 移出 state** —— render 次數確實從 1184 掉到 112，但同 session 交錯 A/B 顯示 script **17ms → 24ms**；加 rAF 合併更糟（27ms）。**Preact 處理「一個元素的一個 style 屬性變了」比手寫同樣的寫入還快。** 退回 state 版，只留下「卡片寬度改成拖曳開始時量一次」——那消掉了每幀一次的 `offsetWidth` 強制排版。
- **`web-vitals` 套件** —— 下載 3.08 KB gzip，然後把每個數值丟掉（沒有 endpoint），而 Cloudflare Web Analytics 一直在收同樣的東西還附帶元件選擇器。刪掉。

真正有效的是**重用 vnode 物件**：Preact 在 `newVNode._original == oldVNode._original` 時整棵跳過（元件跳過 render、元素重用 DOM），所以 `useMemo` 包住 JSX 就有真記憶化，不需要 `memo()` 也不用把 `preact/compat` 拉進 bundle。一次拖曳 304 → 76 次 render，開關 modal 從整頁變成只有 App 一次，打字時的 126 次 Icon render 變成 0。

首屏的修法同時解決 SEO：靜態骨架讓爬蟲從 0 個字變成 249 個英文字元，而 FCP 從「等 JS」變成「等 CSS」（同一次載入內量測：FCP 相對 JS 到手從 **+40ms 變成 −5ms**）。代價是它引入了一個 FOUC——`html` 沒有背景，而 `--bg` 在外部樣式表裡，以前 `#app` 是空的所以看不見。行內腳本現在也負責畫底色。

預設語系改成英文，因為**被 bundle 的那個語系就是「你的語系到之前看到的東西」**，包括分頁標題。順帶初始 JS 從 19.5 掉到 18.8 KB（ASCII 比 CJK 壓得好）。

卡片飛出的動畫也是同一類錯誤：飛行距離用 `window.innerWidth`，但卡片被 `max-width: 40rem` 封住，所以 2560px 螢幕上它會飛 4.2 張卡寬、轉 51 度、速度是手機的六倍。而它**根本不需要飛出螢幕**——卡片同時在淡出，消失是 opacity 的工作。改成卡片自己寬度的 1.1 倍並限制傾斜。

### 效能預算的實際結果

計畫估自然體積 40.5 KB、上限 60 KB。**實際初始 JS 為 19.2 KB**，餘裕 40.8 KB。主要來自兩個決定：Preact 加 compat 別名（省下約 38 KB）、自己寫 i18n 而非引入函式庫（省下約 39 KB）。

預算閘門在開發過程中實際擋下四次：React、i18next、gtag.js、Prism（Markdown 語法高亮，17.4 KB gzip——等於整個 App 的體積）。

測試從 120 成長到 221 個，其中 20 個守著那些憑手感調出來的手勢閾值——那是整個 App 最後一塊沒有測試覆蓋的複雜邏輯。
